/**
 * This app talks to exactly one WordPress site — there's no "connect"
 * screen or user-entered URL/key. Get the API key from that site's
 * wp-admin: Settings > Invoice Settings > Mobile App.
 */
export const API_BASE_URL = 'https://ngatinyore.co.zw';
export const API_KEY = 'REPLACE_WITH_KEY_FROM_WP_ADMIN';
